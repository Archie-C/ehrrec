name: "micron"

# training hyperparameters
epochs: 40
lr: 0.0002

# DDI loss
loss_weights: [0.25, 0.25, 0.25, 0.25]

# logging + checkpoint saving
save_dir: "saved/MICRON"
log_level: INFO
show_progress: true